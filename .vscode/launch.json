{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "shell",
            "request": "launch",
            "name": "Run gitp.sh",
            "command": "${workspaceFolder}/gitp.sh"
        }
    ]
}
